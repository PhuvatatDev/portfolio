---
---

<section class="h-screen relative">
  <!-- Hero text, bottom-left -->
  <div id="hero-text" class="absolute top-[28%] lg:top-auto lg:bottom-[15%] left-[8%] z-20 max-w-[90%] lg:max-w-[50%]">
    <!-- Tagline -->
    <h1 class="font-serif font-bold leading-[1.1] tracking-tight text-charcoal mb-6 lg:mb-8 max-w-[85vw] lg:max-w-[35vw]" style="font-size: clamp(2.2rem, 4vw, 4.5rem);">
      I build products from architecture to deployment.
    </h1>
    <!-- Sub-text -->
    <p class="font-mono text-charcoal/50 mb-8 lg:mb-10" style="font-size: clamp(0.85rem, 1vw, 1rem); letter-spacing: 0.08em;">
      Flutter · Firebase · Clean Architecture
    </p>
    <!-- CTA buttons -->
    <div class="flex gap-4 flex-wrap">
      <a href="#work" class="font-mono no-underline px-6 py-3 bg-pop-yellow text-charcoal font-bold rounded hover:bg-pop-yellow-deep transition-colors" style="font-size: clamp(0.8rem, 1vw, 0.95rem);">
        [ see my work ]
      </a>
      <a href="#contact" class="font-mono no-underline px-6 py-3 border-2 border-charcoal/20 text-charcoal/70 rounded hover:border-charcoal/40 hover:text-charcoal transition-colors" style="font-size: clamp(0.8rem, 1vw, 0.95rem);">
        [ get in touch ]
      </a>
    </div>
  </div>
</section>
